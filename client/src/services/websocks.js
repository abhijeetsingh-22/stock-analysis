// import {io} from 'socket.io-client';

// const socket = io('ws://localhost:8000');

// socket.on('message', (msg) => console.log('message is ', msg));
// socket.on('connect', () => console.log('conneted to ws server'));
// export {socket};
